apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-charging-deployment.fullname" . }}
  labels:
    {{- include "smart-charging-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart-charging-deployment.selectorLabels" . | nindent 4 }}
